using System;

namespace _2019_09_09
{
    /*
        Given three 32-bit integers x, y, and b, 
        return x if b is 1 
           and y if b is 0, 
        using only mathematical or bit operations. 
        You can assume b can only be 1 or 0.
    */
    class Program
    {
        static void Main(string[] args)
        {
            var x = 5;
            var y = 10;
            var b = 0;

            Console.WriteLine(b ^ y || );
            Console.ReadKey();
        }
    }
}
